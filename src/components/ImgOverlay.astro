---
import PortfolioImage from "@components/PortfolioImage";
import { Image } from "astro:assets";
import { imgMap } from "@utils/constants";

const { titleShort, src, alt } = Astro.props;
console.log(imgMap[src]);
---

<div class="dark:bg-dark-overlay-primary absolute inset-0 z-0"></div>
{
  titleShort !== "portfolio" ? (
    <Image src={imgMap[src]} class="rounded-sm w-full h-full" alt={alt} />
  ) : (
    <PortfolioImage alt={alt} client:load />
  )
}
