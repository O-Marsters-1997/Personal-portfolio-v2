{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "cd personal_site && bun install --frozen-lockfile",
  "buildCommand": "cd personal_site && bun run build",
  "outputDirectory": "personal_site/.vercel/output/static"
}
